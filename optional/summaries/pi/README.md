# Optional Pi prose summaries

Not required to open the desk.

Uses the user's existing Pi login at `~/.pi/agent/auth.json`. Locus never copies those tokens into SQLite.

If this folder has no packages, Locus will also look in `~/.pi/agent/npm/node_modules`.
