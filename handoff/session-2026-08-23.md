# Session handoff — 2026-08-23

Work from `/Users/abhigyan/Desktop/Dev/locus`. Do not commit unless asked. Do not touch `~/.pi`.

Run: `npm run dev` → http://127.0.0.1:8787 (Vite 5173, API 8787). Kill both ports before restart or leftover Vite 502s the splash. Pane name: `locus`.

Data: `~/Library/Application Support/Locus/locus.db`. Extension: reload unpacked `extension/shell`.

Tests last green: 28. `npm test && npm run typecheck`.

## What this session did

Desk is a card wall (no item detail page). Two exits: original post vs outbound link. Tags on cards; Auto-tag via read-only `piComplete` (not full Pi SDK).

**Dates:** wall groups only by `published_at`. Missing = Undated. Never use import time as the post date.

**X:** pack + fxtwitter enrich after ingest (`server/enrich.ts`). Card/quote URLs stay in body. Refresh must `mergeKeptUrls`.

**Pi Auto-tag:** xAI OAuth has no builtin `getOAuthProvider`; we use the stored access token. Prefer `opencode-go/deepseek-v4-flash` (catalog is stale; live Go API has it). Skip Zen `opencode`. Tag every untagged row, 8 per call, 4000 tokens (reasoning models ate 1200 on 40 posts).

**Instagram / Reddit capture (extension is the path they use):**
- `extract.js` + `sw.js`: scroll the saved list, harvest **unique IDs** (IG virtualizes ~30 tiles; counting DOM nodes stops early), skip IDs that already have a real body (`GET /capture/v1/known`), read new ones in a **background tab**.
- Thin body = empty or a lone URL → still visit.
- IG images: pick large post media in `article`, skip header/`t51.2885-19` avatars/`fbsbx` gifs. Desk shows **one** IG image.
- Card click opens the post. Clamped caption has **Read more**.

Packs also visit each permalink after listing (Refresh / runner). Runner `maxItems` 300.

## Library right now (approx)

| source | rows | notes |
|---|---|---|
| x | 89 | 88 dated, 65 with http in body |
| reddit | 96 | `published_at` still null until a **new** import (old runs were listing-only). JSON 403, no server backfill. |
| instagram | 134 | ~71 with caption; ~63 link-only. Last import only “read 29” because of virtualization (fixed, not re-run). |
| youtube | 0 | pack still has no listing dates; watch-page `datePublished` enrich exists |

## Do next if they ask

1. Reload extension, IG Import again — should climb past 29 and skip the 71 captioned.
2. Reddit saved Import again — dates + selftext + images.
3. Existing IG media in DB still has leftover creator thumbs until display filter / a re-read. Display already slices to 1.
4. Captions sometimes land as `N likes, N comments - user on Date` (og:description). Real caption is `h1` when present.
5. Auto-tag the rest if they want.

## Don’t

- Invent dates from IG shortcodes or Reddit IDs.
- Fetch Instagram/Reddit from the server (login wall / 403).
- Use `createAgentSession()` or write into `~/.pi`.
- Preview native permalinks as OG cards (stampedes IG and freezes the desk).
